name: Counter
description: A new Flutter bloc / cubit example.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  nuvigator: ^0.6.2
  flutter_bloc: ^6.0.4
  provider: ^4.3.2

dev_dependencies:
  build_runner: ^1.10.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
